// Middleware shoudl verify user sign up data eg email, password, etc
const verifyUserSignupData = (req, res, next) => {};

// Middleware should check if user already exists
const verifyUserExists = async (req, res, next) => {};
